# 2026-07-17 工作日志

## 博客历史文章 frontmatter 迁移（改进点 #1 第二步）
- 任务：批量补齐 83 篇历史文章中缺失的 `description` 和 `lastmod`。
- 工具脚本（落仓库，可复用）：
  - `/Users/chenweilong/blog/temp/inspect_frontmatter.py`：只读检查，统计缺失字段/日期格式。
  - `/Users/chenweilong/blog/temp/migrate_frontmatter.py`：迁移脚本，默认 dry-run，`--apply --backup` 真正写入并备份原始到 `temp/migration_backup/`。
- 执行结果（已 apply + 备份）：
  - 缺失 description：81 → 0；缺失 lastmod：67 → 0；Has BOTH：1 → 82。
  - 跳过 `content/posts/index-about.md`（About 页，本身无 frontmatter，零风险保留不动）。
  - 仅「追加」缺失字段，**不改动任何已有字段、不重排顺序**；`lastmod` 直接镜像各文现有 `date`（三种格式原样保留）。
  - `description` 取自正文首段真实文字（跳过截图/代码/标题/纯URL行），过短的回退到 title；处理过裸URL被 markdown 包裹（如 `*url*`）、纯URL行、短文本等边界。
- 验证：复用 inspect 脚本确认 0 缺失；抽查 `Centos7安装nodejs.md` frontmatter 正常。
- 注意：date 字段仍为三种历史格式（带时分秒 / 纯日期 / ISO+TZ），未强制统一（用户之前只要求补 description+lastmod，统一日期属后续可选步骤）。

## 统一 date / lastmod 为 YYYY-MM-DD（改进点 #1 第三步）
- 任务：把 date 三种格式统一为 `YYYY-MM-DD`；因 lastmod 原镜像 date，同步归一两者保持一致。
- 脚本：`/Users/chenweilong/blog/temp/normalize_dates.py`（dry-run 默认，`--apply --backup` 写入并备份到 `temp/normalize_dates_backup/`）。
- 结果：82 篇（除 index-about.md 无 frontmatter 跳过）date/lastmod 全部统一为 `YYYY-MM-DD`，0 缺失、82 篇两字段齐全。
- ⚠️ 踩坑与修复：初版重建 frontmatter 时用了 `"---\n" + new_fm + "---\n" + body`，而 split_frontmatter 捕获的 fm 不含结尾换行，导致闭合 `---` 与前一行粘连（如 `2023-04-07---`），67 个被改文件 frontmatter 被破坏（inspect 误报 59 个无 fm）。已用预写备份 `normalize_dates_backup/` 全量还原（cp -f），修复脚本为 `"---\n" + new_fm.rstrip("\n") + "\n---\n" + body` 后重跑成功。
- 经验：重建 frontmatter 时**必须**保证闭合分隔符前有一个换行；任何批量改写 markdown frontmatter 的脚本都应先 dry-run + 备份，写后立刻用只读 inspect 复查（确认「无 fm 数」不异常增长）。备份目录按步骤独立存放，便于分步回滚。

## 标签规范化（改进点 #1 第四步）
- 任务：把历史中文/大小写/拼写变体标签映射到 v1 词典（全小写英文、专有名词保留原写法、一个概念一个写法）。
- 先写 `temp/inventory_tags.py` 只读盘点全站 tags：75 个唯一标签，含 6 个中文、多处大小写、3 个拼写错（Liunx/Ubutun/Promethues）。
- 脚本：`temp/normalize_tags.py`（dry-run 默认，`--apply --backup` 写入，备份到 `temp/normalize_tags_backup/`）。
- 映射要点：大小写归一小写（AI→ai、Linux→linux、Go→go、Docker→docker、Prometheus→prometheus、MacOS/MacOs→macos、Zabbix→zabbix、MySQL→mysql、Python→python、Raycast→raycast、Redis→redis、Grafana→grafana、Laravel→laravel、Kubernetes→kubernetes、Hexo→hexo、Composer→composer、GORM→gorm、Obsidian→obsidian、Windows→windows、SSH→ssh、SSL→ssl、VM→vm、iOS→ios、Emby→emby、Arc→arc、Dia→dia、Fellou→fellou、Shadowrocket→shadowrocket、SNMP→snmp、Tools→tools、AlertManager→alertmanager、Container→container、Nodejs→nodejs、M3DB→m3db、NetWork→network、LANSpeedTest→lanspeedtest、LLM→llm、Socket→socket、Skill/skill→skill、agent/agents→agent）；拼写纠错 Liunx→linux、Ubutun→ubuntu、Promethues→prometheus；中文转英文 工具→tools、开发工具→devtools、提示词→prompt、日记→diary、部署→deploy、API网关→api-gateway；同篇去重；统一为内联 `tags: [a, b, c]`（支持 block 风格 2 篇转内联）。
- 结果：69 篇被改，唯一标签 75→64，0 中文、0 大写、0 拼写错；13 篇本已规范未动；index-about.md 无 tags 跳过。复用安全重建（闭合 `---` 前保证换行）。
- 注意：本次仅做「现有标签规范化」，未给每篇补 v1 要求的「主分类(8选1)」必填项；那步更主观（需给每篇归类），可作为后续可选步骤。
